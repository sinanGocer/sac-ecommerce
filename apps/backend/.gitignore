/dist
.env
.env.test
/.inventory-costing-integration-build
.DS_Store
/uploads
/node_modules
yarn-error.log

.idea

coverage

!src/**

./tsconfig.tsbuildinfo
medusa-db.sql
build
.cache

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.medusa

# Product Sync Engine — üretilen raporlar
/sync-reports

# Search Projection — dry-run raporları
/search-reports

# Metadata V2 Enrichment Planner — dry-run raporları
/metadata-v2-reports

# Catalog Batch Pipeline — birleşik run raporları
/catalog-pipeline-reports

# Catalog Batch Pipeline — izole test derleme çıktısı
/.catalog-test-build

# Catalog Product Quarantine — dry-run/commit raporları
/catalog-cleanup-reports

# Catalog Product Quarantine — izole test derleme çıktısı
/.catalog-quarantine-test-build

# Catalog Editor RBAC — izole test derleme çıktısı
/.catalog-editor-rbac-test-build

# Lot Costing — izole test derleme çıktısı
/.inventory-costing-test-build

# Salon Seed Cleanup — dry-run raporları
/salon-seed-cleanup-reports

# Salon Seed Cleanup — izole test derleme çıktısı
/.salon-seed-cleanup-test-build
/.salon-seed-cleanup-commit-test-build

# Env validation — izole test derleme çıktısı
/.env-validate-test-build

# Competitive Pricing — dry-run raporları + izole test derleme çıktısı
/pricing-intelligence-reports
/.pricing-intelligence-test-build

# Cimri Feed — izole test derleme çıktısı
/.cimri-feed-test-build

# User-Assisted Import — raporlar + izole test derleme çıktıları
/assisted-import-reports
/.assisted-import-test-build
/.category-discovery-test-build
/.category-enrich-test-build

# User-Assisted Import — kullanıcı giriş dosyaları + üretilen çıktı (örnek/README hariç)
/import-input/*.txt
/import-input/*.csv
/import-input/*.html
!/import-input/*.example.txt
!/import-input/*.example.csv
/import-input/aveda-discovered-products.csv
/import-input/categories/*
!/import-input/categories/README.md

# Search Projection — izole test derleme çıktısı
/.search-projection-test-build

# Cart Line Cleanup — dry-run/commit raporları
/cart-cleanup-reports

# Cart Line Cleanup — izole test derleme çıktısı
/.cart-cleanup-test-build

# TR Shipping Setup — dry-run/commit raporları
/shipping-setup-reports

# TR Shipping Setup — izole test derleme çıktısı
/.shipping-setup-test-build

# Checkout Test Order — dry-run/commit raporları
/checkout-test-reports

# Checkout Test Order — izole test derleme çıktısı
/.checkout-test-build

# Partial Cart Cleanup — dry-run/commit raporları
/partial-cart-reports

# Partial Cart Cleanup — izole test derleme çıktısı
/.partial-cart-test-build

# Test Order Cancel — dry-run/commit raporları
/test-order-cancel-reports

# Test Order Cancel — izole test derleme çıktısı
/.test-order-cancel-test-build

# iyzico Payment — dry-run raporları
/iyzico-payment-reports

# iyzico Payment — izole test derleme çıktısı
/.iyzico-payment-build
